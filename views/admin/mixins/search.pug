mixin search()
  form(
    id="form-search")
    input(
      type="text"
      name="keyword"
      class="form-control me-2"
      placeholder="Nhập tên sản phẩm..."
      value=keyword
    )
    button(
      type="submit"
      class="btn btn-primary"
    ) Tìm